import styled from 'styled-components';

export const Input = styled.input`
  background-color: #fff;
  height: 40px;
  border-radius: 10px;
  padding: 0 10px;

  font-weight: bold;
  font-size: 10;
  border-width: 0;
  overflow: hidden;
`;
